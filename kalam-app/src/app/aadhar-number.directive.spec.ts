import { AadharNumberDirective } from './aadhar-number.directive';

describe('AadharNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AadharNumberDirective();
    expect(directive).toBeTruthy();
  });
});
